# Pinned IP on the wg-hobby-public bridge - this is the address
# oci-hobby-wg-public's WG_DEFAULT_DNS points clients at (see that
# stack's .env). The two values must match.
DNS_DOH_BRIDGE_IP=172.30.20.53
