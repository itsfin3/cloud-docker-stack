services:
  oci-monitoring-prometheus:
    image: prom/prometheus:latest
    container_name: oci-monitoring-prometheus
    restart: unless-stopped
    ports:
      - "127.0.0.1:9090:9090"
    volumes:
      - /srv/docker/monitoring/oci-monitoring-prometheus/config/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ${TSDB_PATH}:/prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=${RETENTION_TIME}
    networks:
      - server-net

  oci-monitoring-node-exporter:
    image: prom/node-exporter:latest
    container_name: oci-monitoring-node-exporter
    restart: unless-stopped
    pid: host
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - --path.procfs=/host/proc
      - --path.sysfs=/host/sys
      - --path.rootfs=/rootfs
    networks:
      - server-net

  oci-monitoring-cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: oci-monitoring-cadvisor
    restart: unless-stopped
    privileged: true
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
      - /dev/disk:/dev/disk:ro
    networks:
      - server-net

networks:
  server-net:
    external: true
