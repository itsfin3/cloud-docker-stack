# Pinned IP on the wg-hobby-private bridge - this is the rendezvous address
# wg-main pushes to its clients as WG_DEFAULT_DNS (see that stack's .env).
# The two values must match.
DNS_RELAY_BRIDGE_IP=172.30.10.20
