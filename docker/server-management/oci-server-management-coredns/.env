# Pinned IP on server-net - must match the value set for the dnsmasq relay
# stack (DNS_RELAY_BRIDGE_IP) it replaces, and wg-main's WG_DEFAULT_DNS.
# Placeholder only - real value set in Portainer UI, not read from this file.
DNS_RELAY_BRIDGE_IP=172.18.0.53
